{
  "name": "quiz-generator",
  "version": "1.0.0",
  "description": "Root package for Quiz Generator to easily run frontend",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install:frontend": "npm install --prefix frontend"
  }
}
